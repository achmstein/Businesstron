<Project>
  <!-- Shared C# defaults. TargetFramework is declared per-project so this file
       does not interfere with the JavaScript frontend.esproj under the repo root. -->
  <PropertyGroup>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
  </PropertyGroup>
</Project>
